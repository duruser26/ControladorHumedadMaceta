from controlar_humedad_maceta import maceta_necesita_regada

if maceta_necesita_regada() != False:
    pass

# La intención en este código es que detecte que maceta, de que posición necesita ser regada
# En función a Controlar_humedad_maceta